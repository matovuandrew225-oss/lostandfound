# Pass this at runtime with --dart-define or a local dart-define file.
# The backend owns authentication, PostgreSQL, storage, and private secrets.
API_BASE_URL=https://your-replit-backend.example/api
